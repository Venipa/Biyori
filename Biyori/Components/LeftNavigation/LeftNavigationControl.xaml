<UserControl x:Class="Biyori.Components.LeftNavigation.LeftNavigationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Biyori.Components.LeftNavigation"
             xmlns:lib="clr-namespace:Biyori.Core.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Components/LeftNavigation/LeftNavigationControl.styles.xaml" />
    </UserControl.Resources>
    <Grid>
        <ListBox SelectedItem="{Binding SelectedTab, Mode=TwoWay}" ItemsSource="{Binding Tabs, Mode=OneWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
