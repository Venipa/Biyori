{
  "name": "biyori-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd apps/biyori dev",
    "build": "bun run --cwd apps/biyori build",
    "release": "bun run --cwd apps/biyori build:win",
    "release:compile": "bun run --cwd apps/biyori release:compile",
    "release:pack:win": "bun run --cwd apps/biyori release:pack:win",
    "typecheck": "bun --filter \"biyori\" typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@types/bun": "latest",
    "lefthook": "^2.1.10",
    "vite-plugin-svgr": "^5.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.14.0",
    "bun": ">=1.4.0"
  },
  "packageManager": "bun@1.4.0"
}